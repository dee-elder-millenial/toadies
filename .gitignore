# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# Secrets — never commit
.env
*.env

# Local runtime data — never commit
*.db
*.db-wal
*.db-shm
dataset.jsonl
.local/
localai/models/

# Editor/OS
.DS_Store

# secrets / sync / scratch (never commit)
localai/caddy/certs/
*.key
*.sb-*
HeyToadette.txt*
localai/localhost.crt
